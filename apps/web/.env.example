# API Server URL
NEXT_PUBLIC_API_URL=http://localhost:8787

# GitHub App slug used for install URLs
NEXT_PUBLIC_GITHUB_APP_SLUG=cloude-code-local

# Base64-encoded 32-byte key used to encrypt the session cookie wrapper
# generate with: openssl rand -base64 32
SESSION_COOKIE_SECRET=
