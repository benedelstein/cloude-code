{
  "name": "@repo/scripts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint *.ts",
    "dev": "cloudflared tunnel run --url http://localhost:8787 cloude-code-tunnel"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "tsx": "catalog:",
    "@fly/sprites": "^0.0.1"
  }
}
