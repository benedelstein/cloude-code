{
  "name": "@repo/vm-agent",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/vm-agent.bundle.js",
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "vitest run --passWithNoTests",
    "build": "bun build src/index.ts --outfile=dist/vm-agent.bundle.js --target=bun --minify",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "test:live:agent": "bun run src/test-agent-aisdk.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@repo/shared": "workspace:*",
    "ai": "catalog:",
    "ai-sdk-provider-claude-code": "^3.3.3",
    "ai-sdk-provider-codex-cli": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
